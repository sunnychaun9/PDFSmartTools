<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- App internal files directory -->
    <files-path name="files" path="." />

    <!-- App cache directory -->
    <cache-path name="cache" path="." />

    <!-- External files directory (app-specific) -->
    <external-files-path name="external_files" path="." />

    <!-- External cache directory -->
    <external-cache-path name="external_cache" path="." />

    <!-- Downloads directory - limited to PDFSmartTools subfolder for security -->
    <external-path name="downloads" path="Download/PDFSmartTools" />

    <!-- REMOVED: root-path was exposing entire filesystem - SECURITY VULNERABILITY -->
</paths>
